const {Socket} = require("socket.io");
exports.handle = function(socket = Socket) {
    socket.on("chat message", msg=> {
        io.emit("chat message", msg)
    }
)}
        
exports.uhndl = function(req, res, port) {
    res.redirect(`http://localhost:${port}/home`)
}