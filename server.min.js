const express=require("express"),path=require("path"),app=express(),http=require("http").Server(app),io=require("socket.io")(http),port=process.env.PORT||3e3,back=require("./server/backend");app.use(express.static(__dirname+"/public")),app.get("/join",(function(req,res){})),app.get("/",(function(req,res){back.uhndl(req,res)})),io.on("connection",socket=>{back.handle(socket,io)}),http.listen(port,()=>{console.log(`Socket.IO server running at http://localhost:${port}/`)});